# JSON Binding

This example demonstrates
- POST routes
- Binding JSON fields to struct fields
- Built-in error handling of vodka

## Run

Two ways of running:

**1. Vodka CLI(recommended)**

Use the vodka CLI to enable hot reload on file changes

```bash
vodka
```

**2. Basic command**

```bash
go run .
```
